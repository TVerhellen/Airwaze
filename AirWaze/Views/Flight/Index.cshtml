@model List<FlightListViewModel>


@if (@Model.Count != 0)
{
    <table class="table">
        <thead>
            <tr>
                <th scope="col">Date</th>
                <th scope="col">Time</th>
                <th scope="col">Destination</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var x in @Model)
            {
                <tr>
                    <td>@x.Departure.Date</td>
                    <td>@x.Departure.Hour:@x.Departure.Minute</td>
                    <td>@x.Destination</td>                          
                    <td><a class="btn btn-primary" asp-controller="Statistics" asp-action="Edit" asp-route-id="@x.FlightNr">Edit</a></td>
                    <td><a class="btn btn-primary" asp-controller="Statistics" asp-action="Delete" asp-route-id="@x.FlightNr">Delete</a></td>
                </tr>
            }
            
        </tbody>
    </table>
}
else
{
    <p>No flights planned at the moment.</p>
}


