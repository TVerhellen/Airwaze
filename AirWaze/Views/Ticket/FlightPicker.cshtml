@using AirWaze.Entities
@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model TicketFlightPickerViewModel
<main>
    <div class="slider-height d-flex align-items-center">
<table class="table">
    <tr>
        <th>Destination</th>
        <th>Departure time</th>
        <th></th>
    </tr>
    @foreach (var flight in Model.flightList)
    {
        <tr>
            <td>@flight.Destination.Name</td>
            <td>@flight.Departure</td>
            <td><a asp-action="Payment" asp-route-ID="@flight.FlightNr" onclick="message()" asp-route-ticket="@Model.TicketNr" style="color:black">Select</a></td>
        </tr>
    }
</table></div>
<a class="btn btn-primary" asp-action="Create">Go Back</a>

</main>
<script type="text/javascript">
        function message()
        {
            Swal.fire({                   
                    position: 'center',
                    icon: 'success',
                    title: 'Ticket Created',
                    showConfirmButton: false,
                    timer: 1500                 
                    });
        }
    </script>